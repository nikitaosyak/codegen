<%- locals.modifier ? `${modifier} ` : '' -%>class <%-numericCap-%>Range {
	public readonly <%-numeric-%> min;
	public readonly <%-numeric-%> max;
	public readonly <%-numeric-%> step;

	public <%-numericCap-%>Range(<%-numeric-%> min, <%-numeric-%> max, <%-numeric-%> step) {
		this.min = min;
		this.max = max;
		this.step = step;
	}

	public <%-numeric-%> Clamp(<%-numeric-%> value) {
		if (value < min) return min;
		if (value > max) return max;
		return value;
	}
}